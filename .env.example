# Variables de entorno para el proyecto (copiar a `.env` y ajustar)
DB_HOST=db
DB_PORT=3306
DB_NAME=framworddb
DB_USER=framuser
DB_PASS=framword_pass

# Opcional: cambiar DocumentRoot
APACHE_DOCUMENT_ROOT=/var/www/html/public
# Entrypoint espera a la base de datos hasta que esté disponible (o timeout).
DB_WAIT_TIMEOUT=30
